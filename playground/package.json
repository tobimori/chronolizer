{
  "name": "chronolizer-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --dir .. build && vp dev",
    "build": "pnpm --dir .. build && vp build",
    "preview": "vp preview",
    "deploy": "pnpm build && wrangler deploy"
  },
  "dependencies": {
    "chronolizer": "workspace:*",
    "effect": "4.0.0-rc.112"
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "wrangler": "4.120.0"
  }
}
